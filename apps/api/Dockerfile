# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build orders-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t orders-api`.
FROM docker.io/node:20-alpine

# yan comment ENV HOST=0.0.0.0 and port this 
# ENV HOST=0.0.0.0
ENV PORT=3333
# yan add EXPOSE ${PORT}
# EXPOSE ${PORT}
EXPOSE 3333

WORKDIR /app

RUN addgroup --system api && \
    adduser --system -G api api

COPY dist/apps/api api
RUN chown -R api:api .

# yan add port expose

# yan comment all tryin remove dissapearance on package json error
## You can remove this install step if you build with `--bundle` option.
## The bundled output will include external dependencies.
# RUN npm --prefix api --omit=dev -f install

CMD [ "node", "api" ]
